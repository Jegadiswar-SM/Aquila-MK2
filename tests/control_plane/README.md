# Control-plane tests

Tests in this directory shall verify the Aquila Custom Control Interface,
canonical byte-addressed register map, coupled writes, read backpressure,
invalid addresses, and reset. The executable RTL test is currently maintained
under `tb/` and will be added to the repository regression.
